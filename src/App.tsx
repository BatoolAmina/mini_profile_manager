import { ProfileCard } from './components/ProfileCard';

function App() {
  return (
    <main className="min-h-screen bg-gray-100 dark:bg-black flex items-center justify-center p-4 font-sans transition-colors">
      <ProfileCard />
    </main>
  );
}

export default App;